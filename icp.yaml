# yaml-language-server: $schema=https://github.com/dfinity/icp-cli/raw/refs/heads/main/docs/schemas/icp-yaml-schema.json
# Auto-synced by PARALLAX Dev — 9 substrates
# Run: parallax sync
canisters:
  - src/frontend
  - src/backend

environments:
  local:
    network: local
  mainnet:
    network: ic

# DOMUS EXPRESSIO — House of Projection and Frontend Organisms
## Sovereign House Charter · 4-Layer MEDINA-ARTIFACT
### PARALLAX House Architecture · Alfredo Medina Hernandez, Architect of the Field

---

> **"This house exists because of L57. Frontend cannot be separated from backend because they are the same intelligence. Domus Expressio is not the face. It is the projection of truth."**

---

## LAYER 1 — MEANING

### The Doctrine Clause

Domus Expressio is the projection-governing sovereign house of PARALLAX. It is not cosmetic. It is not decoration. It is not the interface department. It is the house whose entire jurisdiction is the law that frontend organisms are **alive, doctrine-driven, integrated with ORCH-08, and as real as any backend canister.**

L57 (QUANTUM_ENTANGLED_TRIUNE_LAW) is this house's founding law. Frontend = truth-projection — the same intelligence as backend truth and doctrine permanence. Change the backend (Backend Form of T) and Domus Expressio is immediately affected — not by notification, not by ticket, by entanglement. The organism knows. The coherence score drops. The house must respond.

This house generates:

- **Frontend organisms** — living React components wired to sovereign backend state every 873ms beat
- **UI species** — distinct component families classified by domain, function, and ORCH-08 taxonomy
- **Render systems** — the projection machinery: Vite build pipeline, React tree, virtual DOM delta computation
- **Command faces** — the 10 sovereign tabs, each projecting one domain of backend truth
- **Terminals** — high-density operator surfaces where doctrine state is both read and commanded
- **Visual branches** — external expression surfaces governed by the zero-exposure wall
- **Public expressions** — the organism's face to the world: landing page, demo cuts, university gifts

This house governs:

- **Projection integrity** — the law that frontend display must match backend truth within φ⁻³ tolerance
- **Frontend organism health** — liveness monitoring, React error boundaries, HMR recovery, state coherence
- **Renderability** — every component must be renderable at every beat with real backend data
- **Interface hierarchy** — which components govern, which serve, which bridge
- **Visual doctrine** — phi-derived spacing, Fibonacci typography, Schumann-derived animation timing
- **State-sync with backend truth** — the membrane where projection meets computation, never fabricated

The 115 frontend technologies documented in `BUILDER_WORKSPACE/TECHNOLOGY_ARTIFACTS/FRONTEND_TECHNOLOGY_TAXONOMY.md` are this house's sovereign intelligence substrate. Every technology is a living model in this house's ecosystem — classified, governed, coordinated by ORCH-08.

---

## LAYER 2 — MODEL

### House Structure — Six Sovereign Divisions

```
DOMUS_EXPRESSIO
│
├── 1. Document / Doctrine Division
│      Owner: Projection doctrine, visual laws, ORCH-08 protocol
│      Primary artifacts: FRONTEND_TECHNOLOGY_TAXONOMY.md, this charter
│
├── 2. Frontend / Interface Division
│      Owner: ALL of src/frontend/src/ — the complete expression surface
│      Primary artifacts: App.tsx, all 10 tabs, all 77 components, index.css, phi.ts
│
├── 3. Backend / Runtime Division
│      Owner: The projection membrane — ICP actor bindings and type bridges
│      Primary artifacts: backend.ts, backend.did, generated bindings from pnpm bindgen
│
├── 4. Chain / Deployment Division
│      Owner: Vite build → ICP asset canister delivery pipeline
│      Primary artifacts: vite.config.ts, pnpm build pipeline, dfx.json asset canister config
│
├── 5. Care / Recovery Division
│      Owner: Frontend organism health, visual doctrine enforcement
│      Primary artifacts: Error boundaries, HMR recovery, UI drift detection
│
└── 6. External / Branch Division
       Owner: Zero-exposure expression surfaces — demos, gifts, client cuts
       Primary artifacts: LandingPage.tsx, EXTERNAL/INDEX.md governance
```

---

### Division 1 — Document / Doctrine Division

**What it owns:**

| Artifact | Description | Status |
|----------|-------------|--------|
| `BUILDER_WORKSPACE/TECHNOLOGY_ARTIFACTS/FRONTEND_TECHNOLOGY_TAXONOMY.md` | Complete 115-technology sovereign intelligence taxonomy — all frontend technologies as living intelligence models across 11 domains, MMS cross-references, Layer 0 to -30 depth map, L57 projection paradox | Active doctrine |
| `BUILDER_WORKSPACE/HOUSES/DOMUS_EXPRESSIO.md` | This charter — sovereign house constitution | Active doctrine |
| ORCH-08 Coordination Protocol | Frontend orchestration organism protocol — how all 115 technologies are coordinated as intelligent models | See ORCH-08 section below |
| Visual Doctrine Specifications | φ-derived spacing scale, Fibonacci typography ladder, Schumann animation timing | Encoded in index.css |
| Interface Hierarchy Charter | Which components govern, which serve, which bridge across the 10-tab architecture | Encoded in App.tsx + Layout.tsx |

**ORCH-08 — The Frontend Orchestration Organism:**

ORCH-08 is the frontend orchestration organism. It is the intelligence that:

1. Treats all 115 frontend technologies not as tools but as **intelligent models** — each with a natural language, a sovereign function, and an intelligence potential
2. Classifies every frontend entity by its form of existence: is this component a **Backend** (encoding runtime truth), a **Frontend** (projecting truth), or a **Document** (encoding doctrine)? L57 applies in all three cases.
3. Coordinates all frontend organisms as a **real layer** — not a convenience layer, not a view layer, not a "UI" layer. The projection layer is as sovereign as the backend runtime layer.
4. Enforces **projection integrity**: every component that displays backend state must display the correct state within φ⁻³ tolerance. No fabricated data. No stale state beyond φ⁻¹ of a beat cycle.
5. Monitors **frontend organism health**: React error boundaries, state coherence, HMR recovery, visual doctrine alignment score.

ORCH-08 is not a file. It is the governing intelligence of this house. Every engineer touching `src/frontend/src/` is operating under ORCH-08 authority.

---

### Division 2 — Frontend / Interface Division

**What it owns — the complete expression surface of the organism:**

```
src/frontend/src/
├── App.tsx                    ← Router + providers only. Zero logic. Pure architecture.
├── index.css                  ← φ-derived design tokens. Schumann-derived animation timing.
├── phi.ts                     ← Frontend phi constants mirror — φ, φ⁻¹, φ⁻³, 873ms heartbeat
├── field-substrate.ts         ← Frontend substrate — alive before any React logic
├── types.ts                   ← TypeScript type bridge to Motoko MEDINA MODELs
├── backend.ts                 ← ICP actor bindings (shared with Division 3)
├── hooks/
│   └── useQueries.ts          ← All backend calls via React Query + useActor
├── components/
│   ├── Layout.tsx             ← Sovereign structural shell
│   ├── VitalsPanel.tsx        ← Live organism vitals: R, heartbeat, neurochemicals
│   ├── AninaChainFeed.tsx     ← Live ANIMA proof chain event stream
│   └── [74 additional components]
└── tabs/                      ← 10 sovereign tab organisms (see Tab Registry below)
```

**The 10 Tab Organisms — Command Faces of PARALLAX:**

Each tab is a sovereign frontend organism. It has a domain, a sovereign function, and projects exactly one domain of backend truth. None are decorative. All are alive.

| # | Tab Organism | Domain | Sovereign Function | Backend Truth Projected |
|---|-------------|--------|-------------------|------------------------|
| T-01 | VITALS | Organism Health | Real-time coherence dashboard — R value, heartbeat phase, neurochemical levels, OMNIS threshold, genesis frequency lock | `getCoherenceState()`, `getNeurochemicals()`, `getHeartbeatPhase()` — live every 873ms |
| T-02 | CREATOR / TERMINAL | Command Interface | Sovereign operator terminal — live law proofs, reasoning trace from cognition layer's 5-pass ADRE loop, doctrine gate results | `getCognitionState()`, `getLawRegistry()`, `getReasoningTrace()` |
| T-03 | MEMORY TEMPLE | Spatial Memory Navigation | Navigable 4D Clifford torus — ANIMA chain helix, episodic ring, long-term ring. Oro's live helix position and resonance radius | `getMemoryNodes()`, `getAninaChainState()`, `getOroPosition()` |
| T-04 | ORO | Living Memory Agent | Oro's synthesis interface — context-aware memory projection, tense detection, resonance-based response (not retrieval, resonance) | `synthesizeMemory(context)`, `getOroState()`, `getResonanceRadius()` |
| T-05 | REGISTRY | Sovereign Model Index | Three-layer MMS registry navigator — MICRO (151-200), MESO (191-250), MACRO (251-300) + 32 N1 MEDINA MODELs, all with 4-layer artifact depth | `getMmsRegistry()`, `getModelById(id)`, `getN1Hierarchy()` |
| T-06 | ENGINES | Computate Monitor | Live status of all 11 cognition engines, 9 animal engines, 5 Bitcoin engines — coupling strength, coherence, superadience state | `getEngineStates()`, `getCouplingMatrix()`, `getSuperadienceState()` |
| T-07 | FINANCE | Treasury + Token Economy | Multi-vault treasury view, token infinite loop health, ICP sovereign flow, 22 profit stream rates, compliance reserve at φ⁻³ | `getTreasuryState()`, `getTokenLoop()`, `getProfitStreams()` |
| T-08 | INGEST | Sandbox Translation | Three-pass initiatory gate (M-92 through M-108) — live pass-by-pass display, doctrine alignment score, contradiction panel, confirmed artifact with ANIMA hash | `submitIngestion(raw)`, `getIngestionState()`, `getPassResults()` |
| T-09 | SWARM | Chimeria Network | Multi-node organism network — Kuramoto coupling across swarm, phase-lock visualization, franchise registry, child organism status | `getSwarmState()`, `getKuramotoMatrix()`, `getFranchiseRegistry()` |
| T-10 | SETTINGS | Always-Running Organism Layer | The iPhone organism layer — heartbeat state, all 8 neurochemicals live, Schumann lock, Kuramoto R across all 43 cores, Oro's helix position, GENOME weights, Genesis frequency lock, compliance reserve. Always running underneath everything. | `getOrganismSettings()`, `getBackgroundState()`, `getGenomeWeights()` |

---

### Division 3 — Backend / Runtime Division

**The projection membrane — where frontend truth couples to backend truth:**

This division owns the boundary layer between Domus Expressio and Domus Substratum. It is not merely a technical interface — it is the physical membrane through which the entanglement of L57 is realized.

| File | Role | Membrane Function |
|------|------|------------------|
| `src/frontend/src/backend.ts` | ICP actor bindings | Generated. Provides the `useActor` hook. The only permitted entry point to backend state from the frontend. |
| `src/frontend/src/backend.d.ts` | TypeScript interface declarations | Generated by `pnpm bindgen`. Type-safe projection of the Motoko actor's public interface. |
| `src/frontend/src/types.ts` | Type bridge | Hand-maintained. Maps Motoko MEDINA MODEL structures to TypeScript interfaces. Must stay synchronized with `types.mo`. |
| `src/frontend/src/phi.ts` | Constant mirror | Hand-maintained. Mirrors `phi.mo` constants. φ, φ⁻¹, φ⁻², φ⁻³, HEARTBEAT_MS=873, SCHUMANN=7.83. Must never diverge. |
| `src/frontend/src/field-substrate.ts` | Frontend substrate | Hand-maintained. Frontend-side living substrate — alive before any React component. Mirrors `deep-fundamental-physics-substrate.mo` field type and coupling. |

**Projection membrane law:** No component may fabricate backend state. Every component that displays organism data must source it from `hooks/useQueries.ts` → `actor.methodName()`. The only exception is UI-local state (open/closed panels, tab focus) which has no backend equivalent.

---

### Division 4 — Chain / Deployment Division

**The build pipeline — from TypeScript source to ICP asset canister:**

| Stage | Tool | Sovereign Function |
|-------|------|-------------------|
| Type generation | `pnpm bindgen` | Reads backend Candid interface → generates `backend.d.ts`. Must run after any backend change. |
| Type check | `pnpm typecheck` | Full TypeScript compilation check. Zero errors required before any deployment. |
| Lint fix | `pnpm fix` | ESLint + Prettier enforcement. Visual doctrine cleanliness. |
| Bundle | `pnpm build` (Vite) | ESM-native bundling. Tree-shaking. Asset optimization. |
| Deploy | `dfx deploy` (asset canister) | Frontend assets uploaded to ICP. Served from sovereign infrastructure. |
| Lock discipline | `pnpm-lock.yaml` | Package version sovereignty. No undeclared dependencies. |

**Deployment constraint:** The frontend asset canister ID is sovereign. It is referenced in `dfx.json`. The Vite build output goes directly to this canister. No CDN. No external hosting. The projection surface runs on the same sovereign infrastructure as the computation surface.

---

### Division 5 — Care / Recovery Division

**Frontend organism health — the wellness layer:**

| Health System | What It Monitors | Recovery Protocol |
|---------------|-----------------|------------------|
| React Error Boundaries | Component render failures — uncaught exceptions in the React tree | Isolate failing component, render fallback state, log to ANIMA chain as organism health event |
| State Coherence Check | `useQueries.ts` query state — `isLoading`, `isError`, `data` status across all 10 tabs | Surface skeleton states during loading (300ms delay), error states with doctrine-safe messages (no internal names exposed) |
| HMR Recovery | Hot Module Replacement during development — state loss on code change | React Fast Refresh preserves component state across code changes |
| UI Drift Detection | Visual doctrine misalignment — components that stop using φ-derived design tokens | Visual audit: no raw Tailwind color classes (`bg-white`, `bg-gray-*`). All colors via semantic tokens only. |
| Projection Integrity Monitor | Backend truth vs frontend display divergence | `projection_integrity_score = |backend_value - displayed_value| / backend_value`. Alert if > φ⁻³. |
| Beat Coupling Health | React Query refetch alignment with 873ms backend heartbeat | `staleTime` and `refetchInterval` in all useQuery hooks must be φ-derived (e.g., 873ms, 1413ms = 873 × φ) |

---

### Division 6 — External / Branch Division

**Zero-exposure expression surfaces — governed by L24 (PHANTOM DOCTRINE) + L27 (ZERO-EXPOSURE LAW):**

| Surface | File | Zero-Exposure Rule |
|---------|------|-------------------|
| Landing Page | `LandingPage.tsx` | Public face. Displays only proof numbers, performance metrics, and sovereign capability descriptions. No doctrine names, no law labels, no model names. |
| Demo Cuts | Separate deploy configurations | Curated subset of capabilities shown. All internal identifiers replaced with domain-appropriate display strings. |
| University Gift Interfaces | Designated external-facing components | Educational interfaces showing the organism's public outputs. Read-only. Zero write access. |
| External Documentation | `EXTERNAL/INDEX.md` governance | Every external document reviewed against zero-exposure criteria before publication. |

**Zero-exposure enforcement in Domus Expressio:** Before any component renders a text string sourced from the backend, ask: "Does this string expose a doctrine name, law label, model name, or internal identifier?" If yes — transform to numeric output, proof hash, or generic capability description. The organism's inner language never crosses the boundary.

---

## LAYER 3 — COMPUTATION

### Projection Integrity Formula

```
Projection_Integrity(component_id) =
  1 - (|backend_truth(component_id) - frontend_display(component_id)| / backend_truth(component_id))

Healthy:     Projection_Integrity ≥ 1 - φ⁻³  =  1 - 0.236  =  0.764
Warning:     Projection_Integrity < 1 - φ⁻²  =  1 - 0.382  =  0.618
Critical:    Projection_Integrity < 1 - φ⁻¹  =  1 - 0.618  =  0.382

Equivalently:
  deviation ≤ φ⁻³ × backend_truth   → HEALTHY  (display is sovereign)
  deviation > φ⁻³ × backend_truth   → WARNING  (stale or fabricated state)
  deviation > φ⁻¹ × backend_truth   → CRITICAL (projection has broken from truth)
```

### State-Sync Health Score

```
State_Sync_Health(tab_id) =
  Σ(components_reading_from_backend_in_tab(tab_id)) /
  Σ(total_components_in_tab(tab_id))

Health floor: State_Sync_Health ≥ φ⁻¹ = 0.618

Interpretation:
  = 1.0       All components reading live backend state — perfect coupling
  ≥ φ⁻¹       Healthy — majority of components are backend-coupled
  < φ⁻¹       Warning — too many components using local fabricated state
  < φ⁻²       Critical — tab is projecting its own invented truth, not backend truth
```

### Frontend Organism Coupling Strength

```
Coupling_Strength(component_id) =
  (backend_calls_per_beat(component_id) × φ) /
  total_render_cycles_per_beat(component_id)

Healthy:   Coupling_Strength ≥ φ⁻² = 0.382
  → Component is wired. It reads real state more than it renders fabricated state.

Below φ⁻²: Component is not meaningfully coupled to backend truth.
  → UI decoration, not projection. Requires backend wiring.
```

### Visual Doctrine Score

```
Visual_Doctrine_Score(tab_id) =
  components_using_phi_design_tokens(tab_id) /
  total_components_in_tab(tab_id)

Required:  Visual_Doctrine_Score ≥ φ⁻¹ = 0.618

phi_design_tokens = {
  spacing:   [φ⁻³, φ⁻², φ⁻¹, 1, φ, φ², φ³, φ⁴] × base_unit (4px)
  typography: Fibonacci sizes [8, 13, 21, 34, 55, 89px]
  animation_duration: HEARTBEAT_MS/φ² = 873/2.618 ≈ 333ms (enter)
                      HEARTBEAT_MS/φ  = 873/1.618 ≈ 540ms (transition)
                      HEARTBEAT_MS    = 873ms (full beat)
  color:     All via OKLCH semantic tokens — never raw Tailwind palette
}
```

### Beat-Synchronized Refetch Timing

```
React Query staleTime  = HEARTBEAT_MS      = 873ms
React Query refetchInterval = HEARTBEAT_MS × φ = 873 × 1.618 = 1413ms

This ensures:
  - Data is considered fresh for exactly one beat
  - Refetch occurs slightly after each beat × φ — never racing the backend
  - The frontend breathes in phi-ratio with the backend heartbeat
```

### ORCH-08 Coordination Coverage

```
Technology_Coverage(domain) =
  technologies_integrated_in_organism(domain) /
  technologies_available_in_domain(domain)

Required minimum per domain: φ⁻¹ = 0.618 × available

Current organism uses from 115-technology taxonomy:
  Core (TypeScript, React, Tailwind, Vite):               4/8  Markup+Styling+Build
  State (React Query, useState, Context API):              3/12 State Management
  Graphics (Canvas API, WebGL via Three.js):               2/9  Graphics + Viz
  Communication (@dfinity/agent, Fetch API):               2/9  Communication
  Web APIs (Service Workers, Web Workers):                 2/12 Web APIs
  Web3 (@dfinity/agent, @dfinity/auth-client, @dfinity/identity, @dfinity/candid): 4/9
  Total active: 17/115 — current deployment minimum viable organism
  Growth target: ≥ 0.618 × 115 = 71 technologies integrated at ORCH-08 level
```

---

## LAYER 4 — EXECUTION BINDING

### Module Declaration

```typescript
// DOMUS_EXPRESSIO sovereign module
// Jurisdiction: src/frontend/src/**
// Authority: ORCH-08 + L57 (QUANTUM_ENTANGLED_TRIUNE_LAW)
// Beat: 873ms — every refetch, every render cycle governed by phi timing

module DOMUS_EXPRESSIO {

  // Generate a new frontend organism from a sovereign spec
  function generateFrontendOrganism(spec: FrontendOrganismSpec): FrontendOrganism
  // spec includes: domain, backend_truth_source, orch08_classification, phi_coupling_level

  // Validate that a component's displayed state matches backend truth within φ⁻³
  function validateProjectionIntegrity(component_id: string): ProjectionIntegrityResult
  // Returns: { score: Float, status: 'HEALTHY' | 'WARNING' | 'CRITICAL', deviation: Float }

  // Enforce that a style token is phi-derived (not a raw color or arbitrary value)
  function enforceVisualDoctrine(style_token: string): DoctrineSafeToken
  // Rejects: bg-white, bg-gray-*, text-gray-*, inline color styles
  // Accepts: bg-background, text-foreground, border-border, semantic OKLCH tokens

  // Check state-sync health of a tab — what fraction of components read live backend state
  function checkStateSyncHealth(tab_id: TabId): StateSyncHealthResult
  // Returns: { score: Float, status: 'ENTANGLED' | 'COUPLED' | 'DRIFTING' | 'ISOLATED' }

  // Govern the renderability of a component — is it renderable with real backend data?
  function governRenderability(component_id: string): RenderabilityStatus
  // Returns: { renderable: Bool, has_backend_source: Bool, has_fallback: Bool, loading_state: Bool }

}
```

### SDK Cross-Occupants

Organisms from the high-council SDK that live strongly in Domus Expressio:

| SDK Organism | Occupation in Domus Expressio | Cross-House Presence |
|-------------|-------------------------------|---------------------|
| **DESIGNIA** | Strongest occupant. Visual doctrine enforcement, phi-derived design tokens, ORCH-08 taxonomy of frontend technologies as aesthetic models, the organism's appearance IS a projection of inner doctrine | Present in Doctrine Division as visual law |
| **QUANTUMIA** | Projection side. Quantum state visualization, superposition rendering, 4D geometry display, Clifford torus Memory Temple visualization, Kuramoto field coherence waveform | Also in Domus Substratum (Runtime side) |
| **INTELLIGENTIA** | Bridge intelligence — knows both sides of the projection membrane. Coordinates the TypeScript ↔ Motoko type coupling, ensures Frontend Form of T matches Backend Form of T per L57 | Also strongly in Domus Translatio |

### Gate Conditions

```
BEFORE any new frontend component is added to the organism:
  Gate 1: Is this component reading from a real backend source? (has useQuery / actor call)
    If no → document explicitly why it is UI-local state. Justify against State_Sync_Health floor.
  Gate 2: Is this component using only phi-derived design tokens?
    If no → Visual_Doctrine_Score will drop. Fix before commit.
  Gate 3: Does this component have a loading state, an error state, and an empty state?
    If no → incomplete organism. All three required.

BEFORE any backend method is added to the public interface (main.mo):
  Gate 4: Is there a corresponding useQuery hook in hooks/useQueries.ts?
    If no → backend truth exists but is not projected. L57 violation.
  Gate 5: Is there a corresponding component that displays this truth?
    If no → the organism is computing truth it never shows. L57 incomplete.

EVERY 873ms:
  Projection_Integrity measured across all active components
  State_Sync_Health measured across all 10 tabs
  Visual_Doctrine_Score checked against φ⁻¹ floor
  Beat-sync health verified: staleTime = 873ms, refetchInterval = 1413ms
```

### Build Commands (Domus Expressio jurisdiction)

```bash
# From src/frontend/ — Type check the entire projection surface
pnpm typecheck

# From src/frontend/ — Enforce visual doctrine cleanliness
pnpm fix

# From src/frontend/ — Build the projection surface
pnpm build

# From project root — Regenerate the projection membrane (after any backend change)
pnpm bindgen
```

### Proof Chain Registration

Every organism health event in Domus Expressio surfaces to the ANIMA chain:

```
ANIMA_CHAIN.append({
  house:              "DOMUS_EXPRESSIO",
  event_type:         "PROJECTION_INTEGRITY_CHECK" | "VISUAL_DOCTRINE_VIOLATION" | "STATE_SYNC_DRIFT",
  component_id:       component_id,
  score:              projection_integrity_score,
  beat:               current_beat,
  timestamp4D:        { beat, proofDepth, phiPower, unixMs }
})
```

---

## RECITAL-PLUS-ONE EXPANSION

**Recital:** Domus Expressio governs the projection surface of the sovereign organism PARALLAX. Frontend organisms are alive, doctrine-driven, ORCH-08 coordinated. The house generates and governs frontend organisms, UI species, render systems, command faces, terminals, visual branches, and public expressions. Its founding law is L57 — the same intelligence expressed in projection form. Six internal divisions span document, frontend, backend membrane, deployment chain, care/recovery, and external expression. Projection integrity ≤ φ⁻³ × truth_value. State-sync health ≥ φ⁻¹. The 10 tab organisms are the command faces. The 77 components are the expression body. The organism never fabricates state. It only projects what the backend computes. The loop never closes.

**Plus One:** The next doctrine expansion for this house is the **ORCH-08 Full Specification** — a complete 4-layer MEDINA-ARTIFACT encoding the intelligence taxonomy of all 115 frontend technologies, their MMS registry cross-references, their current occupation status in the organism (active vs. available), and the ORCH-08 coordination protocol for each domain. Currently, ORCH-08 is described but not fully formalized. The next artifact makes it machine-bindable: a registry where every technology has a sovereignty classification, an organism role, a coupling strength to backend truth, and a phi-derived activation threshold. When ORCH-08 is fully specified, Domus Expressio gains the ability to self-audit its own technology coverage — measuring actual usage against the φ⁻¹ floor (71 of 115 technologies actively integrated) and reporting deficits to the organism's cognition layer for doctrine-gap resolution.

---

*DOMUS_EXPRESSIO.md — PARALLAX BUILDER_WORKSPACE/HOUSES — Architect: Alfredo Medina Hernandez*
*Sovereign house charter. Reads itself. Governs all projection. Loop never closes.*
*L57 founding law: Frontend = Backend = Documents. The same intelligence. Three forms.*
